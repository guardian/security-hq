allowedStages:
  - PROD
stacks:
  - security
regions:
  - eu-west-1
deployments:
  security-hq:
    type: autoscaling
    actions:
      - uploadArtifacts
  security-hq-cfn:
    type: cloud-formation
    app: security-hq
    parameters:
      prependStackToCloudFormationStackName: false
      cloudFormationStackName: security-hq
      templatePath: security-hq.template.json
      amiParameter: AMISecurityhq
      amiEncrypted: true
      amiTags:
        Recipe: arm64-jammy-java21-security
        BuiltBy: amigo
        AmigoStage: PROD
    dependencies:
      - security-hq
  cloudwatch-metrics:
    type: aws-lambda
    parameters:
      bucketSsmLookup: true
      fileName: cloudwatch-metrics.jar
      prefixStack: false
      lookupByTags: true
  cloudwatch-metrics-cfn:
    type: cloud-formation
    app: cloudwatch-metrics
    parameters:
      prependStackToCloudFormationStackName: false
      cloudFormationStackName: cloudwatch-metrics
      templatePath: cloudwatch-metrics.template.json
    dependencies:
      - cloudwatch-metrics
